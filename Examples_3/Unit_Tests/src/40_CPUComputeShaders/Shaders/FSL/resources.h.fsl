#ifndef RESOURCES_H
#define RESOURCES_H

STRUCT(ComputeTestData) {
    DATA(uint32_t, width, None);
    DATA(uint32_t, height, None);
};

STRUCT(DistanceFieldParams) {
    DATA(uint, width, None);     
    DATA(uint, height, None);
    DATA(float, threshold, None);
};

#endif